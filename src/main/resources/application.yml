spring:
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/projeto_jwt
    password: postgres
    username: postgres
    driverClassName: org.postgresql.Driver
  security:
    token:
      secret: segredo
      expiration_time: 1800